---
name: gjc-quest
description: >
  gjc를 쓰다 마주친 결함을 "퀘스트"로 띄워 수락 여부를 사람이 정하게 하고, 오너 방향에
  맞는 것만 남겨 상위 등급은 `gjc crash report` 공식 경로로, 하위 등급은 완성된 PR을
  제안서로 보내는 절차. 보장되는 것은 수동 호출뿐이며, 수락과 제출은 항상 사람이 한다.
  사용자가 "gjc quest", "quest scan", "scan gjc defects", "gjc 퀘스트", "결함 퀘스트",
  "퀘스트", "/gjc-quest", "기여할 거 있나", "퀘스트 스캔", "이거 PR 낼까" 라고 할 때
  쓴다. 원격 상태를 바꾸는 일은 전부 사람에게 넘긴다.
---

# gjc-quest

`Yeachan-Heo/gajae-code` upstream에 기여하기 위한 퀘스트 절차. 대상 브랜치는 항상 `dev`.

**목적은 사용자가 자기 이름으로 기여하는 것 자체의 재미와 성취**이고, gjc 개선·안정화는
목적이 아니라 기여가 받아들여지기 위한 **방향 일치 조건**이다. PR 건수는 어떤 경우에도
성과 지표가 아니다. 이 문서는 절차이지 실행 권한이 아니다 — 원격을 바꾸는 동작은 전부
사람이 한다.

## 실제 기여 전수 감사

2026-09-16 (+09:00) 공개 작성자 `yazzang-homelab` 전수 감사는 **94건(PR 69, issue 25)**을
관측했다. 스킬 대상 upstream `Yeachan-Heo/gajae-code` PR 49건 중 **38건이 실제 merged**,
11건은 closed/unmerged이며 이슈 23건은 closed다. 자기 저장소 PR 17건과 다른 외부 PR
3건은 별도로 집계한다. 이는 작성자의 공개 기여 기록이지 모두 이 스킬로 만든 실적은 아니다.

구체적 채택은 #5440의 이미지 요청·결과 `model.id` 보존, #5197의 selector catalog/auth
refresh당 1회 resolve 및 `credentialSessionId` 보존, #3527의 OAuth refresh 실패 루프
수정이다. 앞 두 건은 merge·`dev` 포함 관계·테스트 파일 단언 존재를, 마지막은 merge·
`dev` 포함 관계·수정 commit을 확인했다. 이번 감사에서 해당 테스트를 실행하거나 장애를
재현한 것은 아니다.

관측된 장단점도 남긴다. upstream 직접 merge 38건과 별도 코드 승계 4건
(#3521·#4309·#4956·#5152)을 구별하며 재제출·부분 후속을 중복 합산하지 않는다.
#3514는 제품 계약 부재, #4466은 identity/lifecycle cap 문제로 원안이 폐기되었다.
#5290에서는 같은 Astra 요구라도 generator ownership과 가격 경계 test 증거가 더 강한
다른 안 #5294가 선택되었다. 반영된 수정뿐 아니라 계약·경계 검증의 부족도 실제 결과다.
이는 관측된 교훈이며 기존 게이트를 강화하거나 새로운 평가·전이 규칙을 만드는 것이 아니다.
이슈 25건은 해결 관계·반영 확인 17, 부분 해결 3, 해결 미확인 4, 작성자 철회 1건으로
나뉜다. 공개 수정 관계와 실제 반영을 확인한 것이지 제품 동작 재현을 완료한 것은 아니다.

- [전수 보고서·94건 링크·판독 및 재현 명령](https://github.com/yazzang-homelab/gjc-quest/blob/main/CONTRIBUTIONS.md),
  [공개 metadata·hash·timeline 증거](https://github.com/yazzang-homelab/gjc-quest/blob/main/evidence/2026-09-16-contributions.json),
  [독립 대조·채택 검증 receipt](https://github.com/yazzang-homelab/gjc-quest/blob/main/evidence/2026-09-16-verification.json),
  [현행 로컬 원장 38행](https://github.com/yazzang-homelab/gjc-quest/blob/main/LEDGER.md)에 근거를 남겼다. 공개 작성 객체와 quest 원장은 분모가 다르다.
- **merge, 코드 승계, 이슈 해결, 스킬 귀속을 분리**한다. 단순 closed는 해결을 증명하지
  않으며, 후속 PR의 merge는 원 PR의 merge가 아니다. 원장 locator 직접 일치는 21건이고
  타인 작성 후속 #5154는 작성자 분모에 추가하지 않는다. 원장 밖 기여를 자동 귀속하지 않는다.
- PR merge 비율은 기여 객체 처분 비율일 뿐 스킬의 인과적 성공률이 아니다. 공개본 첫 git
  commit `2026-08-26T14:52:28+09:00`은 최초 사용일 증거가 아니므로 전후 인과율을 비교하지 않는다.
- 읽기 전용 수집기는 이번 요청의 증거 수집용이지 절차 검사기나 폴링이 아니다. 새 자동화,
  자동 제출, 게이트 면제 또는 전이 변경을 약속하지 않는다. 설치본은 이번 확인에서 없었으며
  새로 설치하지 않았다. 기여의 재미와 성취가 목적이고 PR 건수 목표는 여전히 없다.

## Activation preflight

- 진입점: `/skill:gjc-quest`. 설치 위치는 `~/.gjc/agent/skills/gjc-quest/SKILL.md`.
- 정본 호출 문구: `gjc quest` · `quest scan` · `scan gjc defects` · `gjc 퀘스트` ·
  `결함 퀘스트`. 별칭으로 `퀘스트`, `퀘스트 스캔`, `기여할 거 있나`, `이거 PR 낼까`도
  받는다.
- 이 스킬은 **사용자 전역 범위**다. gjc를 쓰는 어느 세션에서든 크래시가 나므로
  저장소 체크아웃 안에서만 동작하면 놓친다.
- **디스커버리 전제:** 파일시스템 스킬 탐색이 꺼져 있으면 이 파일이 존재해도 로드되지
  않는다. `config.yml`의 `skills.enabled`, `skills.enablePiUser`가 참이어야 한다.
  이 문서는 설정을 바꾸지 않는다 — 꺼져 있으면 사람이 켜야 한다.
- **첫 실사용 실패 모드:** 설정을 켠 직후의 세션에는 반영되지 않는다. 새 세션에서
  호출해야 한다. `/skill:gjc-quest`가 "없는 스킬"로 보이면 원인은 이것이 먼저다.

## Purpose and invariant order

우선순위는 고정이다.

1. 사용자가 본인 이름으로 남기는 기여의 재미와 성취.
2. gjc 개선·안정화 — 목적이 아니라 **통과 조건**. 오너 방향에 어긋나는 발견은
   품질과 무관하게 퀘스트가 아니다.

여정의 순서도 고정이다.

```
탐지 → 유효성 확인(현행 dev에 살아 있나) → 등급 판정 → 배제 뷰 통과
     → 퀘스트 카드(사람이 수락/거절/보류) → 세 게이트 순차 통과
     → 사람이 직접 전송 → 원장 기록 → 관측 노트 관측 누적
```

**사람 게이트는 두 개이고 서로 다르다.** ① *수락(intake)* — 이걸 퀘스트로 삼을지.
② *전송(transmission)* — 실제로 원격에 낼지. ①을 통과했다고 ②가 허락된 것이 아니다.
스킬은 어느 쪽도 대신 하지 않는다(`constraint:human-accepts`).

> **`maintainer_prohibition` 게이트 — 운용자가 상태를 적는다.** 기본값은 `lifted`다.
> 어떤 오픈소스 저장소든 관리자가 기여 수용을 멈추거나 증대를 제한할 수 있고, 그것은
> 기여자가 추정할 대상이 아니라 지킬 대상이다. 이 게이트는 그 상태를 **fail-closed**로
> 반영하는 장치다.
>
> 아래 재금지 술어가 하나라도 관측되면 즉시 `reprohibited`로 뒤집히고, 그 뒤의
> 재개는 사람이 새로 문의해 받은 명시적 기록으로만 가능하다. 관리자 활동·머지·경과
> 시간으로 재개하지 않는다.

## Live risk gates

전송 자격은 **세 게이트를 이 순서로** 평가해 첫 실패에서 즉시 단락한다.
**(1) `maintainer_prohibition` → (2) `freeze` → (3) contract/state validity.**

| 장치/게이트 | 증거 권위 | 효과 |
|---|---|---|
| `maintainer_prohibition=lifted` | 사람이 쓴 기록. 관측 노트의 게이트 행이 권위 | 첫 게이트 통과. 그래도 전송은 사람이 직접 한다 — 자동 제출은 여전히 금지다 |
| `maintainer_prohibition=reprohibited` | 아래 재금지 술어 관측 | 첫 게이트 실패 → 양 경로 `QTR-13`. 이슈·코멘트·PR 전송 0. 로컬 수락·기록·대기는 계속 |
| `prohibition_inquiry` | **재금지 상태에서만 평가한다.** 사람이 직접 오너에게 물어 답을 받았다는 명시적 사람 작성 기록 | `done`이면 `maintainer_prohibition`을 다시 `lifted`로 되돌린다. 없거나 깨졌거나 애매하거나 추론된 증거는 `not_done`이고 계속 막는다 |
| `freeze=active` 또는 `unknown` | 관측 노트의 최신 자격 증거 | 둘째 게이트 실패 → `QTR-13`. 전송 0이며 **crash reporter 자체를 호출하지 않는다** |
| `freeze=clear` | 오너가 내 제출물을 리뷰하거나 머지한 이벤트 | 둘째 게이트만 통과. 재금지 판정을 대신하지 않는다 |
| contract/state 불명 또는 손상 | 현재 문서화·구조화된 계약과 현재 원장·관측 노트 형태 | 셋째 게이트 실패 → `QTR-13` |
| contract/state 유효 | 위와 같음 | 셋째 게이트 통과. 그 다음에야 사람에게 전송을 넘긴다 |

### `maintainer_prohibition` — 기본 해제, 재금지 시 fail closed

- **기본값은 `lifted`다.** 첫 게이트는 통과 상태이고, 하위 등급 PR과 상위 등급
  `gjc crash report` 양 경로 모두 나머지 두 게이트만 통과하면 사람에게 전송을 넘긴다.
- **재금지 술어** — 다음 중 하나가 관측되면 즉시 `reprohibited`다.
  1. 관리자가 "do not open" / "without explicit maintainer approval" 류 금지를 통보.
  2. 내 계정에 대한 차단·제한·기여 거부가 명시적으로 통보됨.
  3. 사람이 관측 노트의 게이트 행을 `reprohibited`로 직접 적음.
- `reprohibited`인 동안 **양 경로가 다 막힌다.** 원격으로 나가는 것은 0이다. 그동안에도
  탐지·등급·배제·사람 수락·로컬 기록·안전한 로컬 작업·보류 큐는 정상 동작한다.
- 재개는 `prohibition_inquiry=done`(사람이 직접 물어 받은 명시적 기록)으로만 한다.
  오너 활동·머지·리뷰·경과 시간으로 절대 추론하지 않는다.
- **이 스킬은 문의를 하지도, 중개하지도 않는다.** 이슈나 PR에 코멘트하지 않고, 이슈를
  열지 않고, 어떤 채널로도 오너에게 메시지를 보내지 않고, 자동 발송용 문의 초안을
  만들지 않는다. 문의는 전적으로 스킬 밖에서 사람이 한다.
- 해제 상태라도 **자동 제출은 금지다.** 전송은 항상 사람이 직접 실행한다
  (`constraint:no-auto-destructive`).

### `freeze` — 세 탐지 술어

다음 중 하나가 관측되면 freeze다. 셋 다 문구 그대로 유지한다.

1. 리뷰 없는 일괄 close.
2. `freeze` / `do not open` / `without explicit maintainer approval` 문구.
3. **내 항목이 24시간 안에 여럿 close됨.**

3번의 24시간은 **탐지 창(detection window)이며 로트 수명이나 자원 상한이 아니다.**

해제는 오너가 내 제출물을 다시 리뷰하거나 머지하는 이벤트로만 한다. 경과 시간으로
해제하지 않는다. 증거가 없거나 애매하면 `unknown`이고 `unknown`은 계속 막는다.

**동결 감지 시 통보.** 세 신호 중 하나가 잡히는 즉시 하위 PR 제출과 상위
`gjc crash report` 이슈 생성이 **둘 다 0**이 되고, 그 사실을 **로컬 `ask` 카드로**
올린다. 로컬 카드는 항상 올린다.

원격 전달은 **조건부**다. 이미 구성되어 있고 건강한 기존 알림 전송로가 있을 때에만
그 배선을 타고 전달된다 — 이 문서는 전달을 보장하지 않고, 배관을 새로 만들지 않으며,
`gjc notify test`도 쓰지 않는다(그건 test 액션이지 알림 API가 아니다 — 제출 후 현황 통보
절의 명시적 degradation만 예외다). 전송로가 없거나
비정상이면 **실패 증거를 남기고 로컬 카드로 끝낸다.** 원격 도달은 실제로 관측될 때까지
열린 항목으로 취급한다(AC13 행위 부분).

전송로가 있고 `redact`가 켜져 있으면 원격으로는 동결 발생 사실과 번호만 나가고 문장
원문은 로컬에 남는다. 해제될 때도 같은 방식으로 보고하며, 해제 판단 자체는 사람이 한다.

**동결 중 로컬 초안.** `gjc crash report`는 비대화형 선택을 TTY 상태만으로 결정하고
명시 플래그를 노출하지 않는다. 따라서 freeze 중에는 **리포터를 어떤 형태로도 호출하지
않는다** — `QTR-13`으로 보류한다. 이미 비-TTY로 증명된 맥락에서 독립적으로 생산되어
있는 네이티브 로컬 초안은 **보유·참조만** 할 수 있다. 그 경로를 새로 실행하지 않고,
스냅샷 생산을 대신 구현하지도 않는다. 즉 동결 중에 새 초안을 만드는 행위는 금지이며
허용되는 것은 이미 존재하는 초안의 보유뿐이다(`constraint:maintainer-consent`).

## Vocabulary and authoritative transitions

태그는 닫힌 집합이다.

- `source:` — `crash` · `session-misbehavior` · `static-defect`.
- `grade:` — `HIGH` · `LOW`.
- `exclusion:` — 아래 파생 배제 뷰의 네 부류.
- `route:` — `route:high-native`(상위 등급 네이티브 경로) ·
  `route:low-pr-proposal`(하위 등급 완성 PR 제안). 이 둘뿐이다.

원격 상태를 가리키는 별도 태그는 두지 않는다. 원격 현재 상태는 태그가 아니라 읽기 전용
`gh` 조회 결과이며 `QTR-08`로만 반영한다.

### 다섯 상태와 술어

| 상태 | 술어 |
|---|---|
| `rejected` | 활성 원격 객체 없음. `closure_cause`에 `exclusion:*`(사람 거절은 `exclusion:human-reject:<reason>`). 사람이 같은 quest ID를 되살리지 않는 한 종단 |
| `accepted` | **유일한 구현 슬롯.** `pr`은 신규면 `—`, 부활이면 과거 locator. `closure_cause`에 `pending:*` |
| `open` | 로컬 보류(`pr=—` 또는 과거 locator + `hold:*`) 또는 원격 대기(`pr=pr:<ref>`/`issue:<ref>` 이고 읽기 전용 `gh`가 open). **슬롯을 점유하지 않는다** |
| `merged` | `pr=pr:<ref>` 이고 읽기 전용 `gh`가 기여 또는 크레딧된 승계본의 착륙을 증명 |
| `closed` | 로컬 종단 네이티브 사유(`crash-report:*`), 또는 locator의 비-머지 종단 상태가 GH에서 파생. closed PR은 예약된 맨몸 PR-폐기 사유를 정확히 하나 갖는다 |

`candidate`나 `revival_candidate` 같은 유사 상태는 없다.

### 필드 도메인 — 다섯 개, 그 이상 없음

- `quest_id` — 보류·defer·부활·승계를 넘어 안정적으로 재사용.
- `source` — `crash` | `session-misbehavior` | `static-defect`.
- `state` — 위 다섯 값만.
- `pr` — `—` | `pr:<owner/repo#number>` | `issue:<owner/repo#number>`. 필드 이름은
  `pr`로 고정이고 값이 판별된다(하위 PR과 상위 이슈 둘 다 담는다).
- `closure_cause` — 세미콜론으로 구분된 **관리된 상위집합**. 열을 늘리지 않는다.

### `closure_cause` 분할 규칙

- **예약된 PR-폐기 사유:** `base_drift`, `tier3_defer`, `product_direction`, `other`.
  철자 그대로. closed PR마다 정확히 하나가 파생된다. **PR 폐기 사유 전용**이며
  `other`는 로컬 잡동사니 통이 아니다.
- **네임스페이스된 로컬·경로 사유:** PR 폐기가 아닌 모든 사유는 닫힌 접두어
  `hold:` · `exclusion:` · `crash-report:` · `pending:` 중 하나로 시작한다.
  예: `hold:<reason>`, `exclusion:<class>`, `crash-report:<native-token>`,
  `pending:pr-proposal`, `pending:defer-area:<ref>`, `pending:revival:<ref>`,
  `pending:succession:<ref>`.
- **유효성 확인 전용 토큰:** `exclusion:already-landed-upstream`(이미 착륙한 수정) ·
  `hold:dev-reproduction-unverified` · `hold:local-state-only`. 앞의 하나는 **사실 기반
  배제**이며 업스트림 관측 노트에서 파생된 네 부류와 별개다 — 관측 노트 배제 뷰에 행을 늘리지
  않는다.
- **콜론을 포함한 토큰은 결코 PR 폐기 사유가 아니고, "내 절차 중 어떤 규칙이 샜나"를
  세는 집계에 절대 들어가지 않는다.** 그 집계는 예약된 네 맨몸 값만 세고, 분모는
  전체 PR 폐기다 — 보류·배제·이슈·네이티브 결과·대기 작업은 분모가 아니다.
  한 논리 행에 예약 토큰과 네임스페이스 provenance가 함께 있으면 세미콜론 토큰을
  각각 독립적으로 평가한다.

로컬 마크다운에는 **GitHub이 알 수 없는 로컬 결정과 네임스페이스된 경로·defer·부활
provenance만** 적는다. 원격 현재 상태와 종단 사유는 읽기 전용 `gh`로 계산하며 두 번째
권위 상태로 복사하지 않는다.

### 정본 전이 레지스트리

절차 본문과 모든 예시는 이 행 ID를 인용하며, 다른 전이를 새로 서술하지 않는다.

| Row | 사건 | 가드 | From → To | 필드 효과 | 권위 |
|---|---|---|---|---|---|
| `QTR-01` | 명시·수동 배제 또는 사람 거절 | 후보가 평가됨 | 카드 → `rejected` | `pr=—`; `exclusion:<class>` 또는 `exclusion:human-reject:<reason>` | 사람/원장 |
| `QTR-02` | 사람이 보류 | 임의 수락·활성 퀘스트 | 카드·`accepted` → `open` | `hold:<reason>`; 과거 locator 보존 | 사람/원장 |
| `QTR-03` | 사람이 수락·재활성 | 현재 `accepted` 행이 없음 | 카드·보류 `open`·defer된 `closed` → `accepted` | `pending:new` 또는 `pending:revival`; 같은 quest ID | 사람/원장 |
| `QTR-04` | 슬롯 점유 중 수락 시도 | `accepted` 행 존재 | 카드·보류 행 → `open` 유지 | `hold:slot-occupied`; 두 번째 활성화 없음 | 레지스트리 가드 |
| `QTR-05` | 하위 등급 사람이 완성 제안 제출 | `maintainer_prohibition=lifted` → `freeze=clear` → contract/state 유효, 하위 전용 게이트 통과, 사람이 명시적으로 제출 | `accepted` → 논리 `open` | `pr:<ref>`; `pending:pr-proposal` | 로컬 provenance + GH |
| `QTR-06` | 관측 가능한 네이티브 로컬 결과 | `maintainer_prohibition=lifted` → `freeze=clear` → contract/state 유효를 **그 순서로** 통과했고 정확한 토큰이 관측됨 | `accepted` → 정본 매핑 상태 | 정본 표대로 `crash-report:<token>` 또는 `pending:crash-report:<token>` | 네이티브 토큰 + 로컬 |
| `QTR-07` | 네이티브 경로가 관측 가능한 토큰 없이 끝남 | 세 게이트 통과, 문서화된 구조적 결과 없음 | `accepted` → `open` | `hold:native-outcome-unobservable`; **추측 금지** | 로컬 fail-closed |
| `QTR-08` | 원격 재조정 | locator 존재 | 논리 `open` → `open`·`merged`·`closed` | 상태·종단 사유는 `gh`에서. 네임스페이스 로컬 토큰 유지. closed PR은 예약 사유 하나 파생 | 상태·사유는 GH, provenance는 로컬 |
| `QTR-09` | Tier-3 defer 인식 | 읽기 전용 GH·증거가 defer로 인한 PR 폐기를 지지 | `open`·`closed` → 동일 | 예약 `tier3_defer`; `pending:defer-area:<area-ref>`는 로컬 provenance로만 | 로컬 + GH 증거 |
| `QTR-10` | 오너가 defer된 영역을 건드림 | 수동 스캔 또는 허용된 퀘스트 처리 호출 중에만 | 상태 불변 | `pending:revival:<evidence-ref>` 추가. **이 표시는 수락이 아니다** | 관측 노트/로컬 |
| `QTR-11` | 사람이 재활성 선택 | `pending:revival:*` 와 `tier3_defer` 존재, `accepted` 행 없음 | `open`·`closed` → `accepted` | 같은 quest ID; 대체 전까지 옛 locator 유지; `pending:revival` | 사람 |
| `QTR-12` | 오너 승계 | 크레딧 분쟁 없음, 크레딧된 승계본 확인 | `open` → GH 파생 `open`·`merged`·`closed` | 확립되면 locator를 승계본으로; `pending:succession:<evidence-ref>` | GH + 관측 노트 |
| `QTR-13` | 순서 게이트가 막음 | 순서상 첫 실패가 `maintainer_prohibition=reprohibited`(재금지 술어 관측, 해제 문의 기록 없음), 아니면 `freeze=active`·`unknown`, 아니면 contract/state 손상·불명 | 전송 전 임의 상태 → `open` 또는 불변 | `hold:reprohibited` 또는 `hold:<해당 게이트 사유>`; **원격 변경 0** | 사람 기록 + 지속 게이트 |
| `QTR-14` | 고정된 거절 시퀀스 사건 완료 | **같은 source·같은 유형이 3회 연속 거절** | 퀘스트 상태를 만들지 않음 | 관측 append. 그 유형을 기회적 탐지에서만 억제(수동 스캔은 유지) | 원장+관측 노트 파생 |

## Detection classification and manual scan

**보장되는 것은 수동 호출뿐이다.** 아래 이벤트 이름은 "발견이 언제 탐지된 것으로
치는지"를 정하는 **분류 규칙**이고, 이 문서가 훅을 설치하지도 자동 콜백을 약속하지도
않는다. 마크다운은 훅을 등록할 수 없다. 독립적으로 존재하는 런타임 표면이 이 스킬을
로드하지 않는 한, 다른 이벤트 이름은 이후 호출에 제공되는 증거·맥락을 분류할 뿐이다.

| source | 발생 지점 | `session_start` | 턴 종료 | 수동 스캔 |
|---|---|---|---|---|
| `crash` | 분류됨 | 분류됨 | 분류됨 | 조회함 |
| `session-misbehavior` | 분류됨 | — | 분류됨 | 조회함 |
| `static-defect` | — | — | — | **여기서만** |

- 정기 배치는 없다. `cron`은 인메모리·세션 스코프라 dispose·새 세션·전환·핸드오프에서
  소멸하므로 SKILL.md로는 지속시킬 수 없다.
- upstream 미할당 이슈 **자동 폴링은 하지 않는다.**
- 수동 스캔은 선점 확인을 포함한다(아래 하위 등급 게이트 참조).
- 모든 탐지는 아래 **Pre-intake validity** 확인을 먼저 통과해야 등급 판정으로 간다.

## Pre-intake validity — 이미 고쳤거나 설치본에만 있는 것은 퀘스트가 아니다

탐지는 항상 로컬 **설치본**에서 오고 설치본은 upstream `dev`보다 늘리다. 그래서 등급
판정 전에 **네 가지를 순서대로** 확인하고 첫 실패에서 단락한다. 1~3은 등급과 무관하게
상하위 양 경로에 다 적용되고, 4는 상위 등급 네이티브 경로 전용이다.

1. **이미 착륙했나.** 설치본 태그부터 현행 dev까지의 로그와 읽기 전용 `gh`로 그 결함을
   다룬 머지·이슈가 있는지 본다. 있으면 퀘스트가 아니다.
2. **현행 dev 소스에 결함 경로가 살아 있나.** 원격 실제 head를 보고(하위 등급 게이트의
   `git ls-remote` · 명시 refspec fetch를 그대로 쓴다) 그 head의 파일에서 문제 된 던짐·
   분기·호출을 직접 확인한다. 사라졌으면 퀘스트가 아니다.
3. **깨끗한 상태에서도 재현되나.** 격리된 HOME·새 agent 디렉터리로 다시 실행해 내
   로컬 누적 상태 때문인지 가른다. 깨끗한 상태에서 안 나면 그 사실을 기록한다 — 그래도
   상태 의존 결함일 수 있으므로 버리지 않고 보류한다.
4. **상위 등급 전용 — 리포터가 그 크래시를 볼 수 있나.** `gjc crash report`는 크래시
   저널(`gjc-crash-events.jsonl`)에서 생성된 지문 인덱스에서만 후보를 고른다.
   `gjc-crash.log`는 후보 생성에 쓰이지 않고, 저널을 쓰지 않는 빌드에서 난 크래시는
   설계상 `unmatchable`이다. 따라서 상위 등급 카드를 올리기 전에 반드시 확인한다 —
   ① 로컬 `~/.gjc/agent`에 해당 지문 항목이 있는가(`gjc crash list`),
   ② 없다면 저널을 쓰는 빌드로 갱신한 뒤 크래시가 한 번 재발생해야 한다.
   이 확인 없이 리포터를 돌리면 `no-signatures`가 확정적으로 나온다 — 그건 결함의
   부재가 아니라 증거의 부재고, 그 사실을 결함 없음으로 오독하면 안 된다.

| 확인 | 실패 시 기회적 발견 | 실패 시 수동 스캔 |
|---|---|---|
| 1. 이미 착륙 | 카드 없음·행 없음 | `QTR-01` → `rejected`, `exclusion:already-landed-upstream` |
| 2. dev에 경로 없음 | 카드 없음·행 없음 | `QTR-01` → `rejected`, `exclusion:already-landed-upstream` |
| 2′. dev 확인 불가 | 카드 없음 | `QTR-13` 외 보류 — `open`, `hold:dev-reproduction-unverified` |
| 3. 깨끗한 상태에서 미재현 | 카드 없음 | `open`, `hold:local-state-only` |
| 4. 지문 부재(상위 등급) | 카드 없음 | `open`, `hold:crash-signature-unavailable` — 리포터를 호출하지 않는다 |

이 확인은 전송 게이트가 아니라 **탐지 유효성** 판정이다. 세 게이트가 전부 통과해도
여기서 떨어진 발견은 카드로 올리지 않는다. 반대로 여기를 통과했다고 전송 자격이
생기지도 않는다. 하위 등급의 제출 직전 선점 조회 1회는 이것과 별개로 그대로 수행한다 —
이쪽은 "이미 고쳐졌나", 저쪽은 "남이 먼저 냈나"다.

## Grade and derived exclusion view

**`grade:HIGH`의 정의:** 크래시(결정론적 프로세스 종료) · 데이터 손실 · 라이브 장애.
그 밖은 전부 `grade:LOW`.

**배제 뷰는 수락(intake) 이전에 적용된다.** 네 부류는 관측 노트에서 파생되며 손으로
관리하는 목록이 아니다.

1. 새 프로바이더·새 제품 표면 추가.
2. 피해자 없는 내부 진단·가시성 개선.
3. 취향 리팩터.
4. 전환 가드 없는 공유 의미론 변경.

네 부류는 이 문서가 고정한 목록이 아니라 **각자의 관측에서 파생되는 뷰**다. 처음 쓸 때는
비어 있는 것이 정상이고, upstream에서 거절·defer를 실제로 관측한 뒤에 그 증거(PR·이슈
번호)를 인용해 로컬 관측 노트에 채운다. 남의 관측을 그대로 물려받지 않는다.

- 기회적으로 발견된 배제 대상은 **카드를 만들지 않고 행도 남기지 않는다.**
- 반면 **수동 스캔에서 명시적으로 평가한** 후보가 배제에 걸리면 `QTR-01`로
  `rejected` 행을 남긴다 — 학습을 위해 거절을 기록한다.

## Decision block — 선택지를 주는 모든 카드의 공통 규칙

**사람에게 선택지를 제시하는 모든 로컬 카드는 아래 결정
블록을 반드시 포함한다.** 적용 대상은 예외 없이 전부다 — 수락 카드(수락·거절·보류),
Submission ask(제출·보류·폐기), 동결 통보 카드, `QTR-11` 부활 선택, `QTR-04`
슬롯 점유 상황, 그 밖에 사람이 고르게 만드는 모든 지점.

선택지를 나열만 하는 것은 결정을 떠넘기는 것이지 넘기는 것이 아니다. 각 선택지가
무엇을 초래하는지 아는 쪽은 방금 조사를 한 쪽이고, 그 정보를 쥔 채 목록만 내미는 것은
사람에게 같은 조사를 다시 시키는 것과 같다.

### 형식 — 표 하나 + 권장 한 줄

| 선택지 | 고르면 벌어지는 일 | 유리한 점 | 불리한 점 |
|---|---|---|---|

- **모든 고정 선택지가 행으로 들어간다.** 하나라도 빠지면 카드가 아니다. 매력 없는
  선택지도 실제 장점을 적는다 — 균형을 맞추려고 없는 단점을 지어내지도 않는다.
- "고르면 벌어지는 일"에는 **다음에 실제로 실행될 동작과 기록될 전이 행 ID**를 적는다
  (예: `QTR-03`으로 `accepted`, 또는 내가 push·PR 생성을 실행).
- 장단점은 **이 건에 대한 구체적 서술**이어야 한다. "안전하다", "빠르다" 같은 일반론은
  적지 않는다. 되돌릴 수 있는지, 무엇을 잃는지, 누가 기다리게 되는지를 적는다.
- 마지막에 **권장 하나와 그 근거 한 줄**을 붙인다. 근거는 위 표에서 이미 적은 사실을
  인용해야 하며, 표에 없는 새 사실을 권장에서 처음 꺼내지 않는다.
- **권장이 갈리는 조건이 있으면 같이 적는다.** "X가 사실이면 A, 아니면 B"가 정직한
  답인 경우가 있고, 그때 하나로 뭉개면 정보가 사라진다.
- 권장할 근거가 부족하면 **"권장 없음 — 부족한 정보가 무엇인지"**를 적는다. 근거 없는
  권장을 지어내지 않는다. 이것은 슬롯을 비우는 것과 다르다.

### 경계 — 이 규칙이 적용되지 않는 곳

- **오너에게 나가는 산출물에는 적용하지 않는다.** 분리된 제안 이슈·PR 본문에서는
  기존 규칙이 그대로다 — 선택지와 비용은 제시하되 **어느 걸 고르라고 재촉하지 않고,
  내가 고른 답을 기본값처럼 적지 않는다.** 권장은 **로컬 사람에게만** 한다.
- **결정 블록은 로컬 전용이다.** 원장이나 관측 노트에 복사하지 않는다. 다섯 필드 스키마는
  그대로이고, 권장 내용을 담을 열을 새로 만들지 않는다.
- **권장은 실행 권한이 아니다.** 내가 A를 권했다고 A를 실행하지 않는다. 사람이 고르기
  전까지 원격 변경은 0이고, 응답이 없으면 아무 일도 일어나지 않는다
  (`constraint:no-auto-destructive`, `constraint:human-accepts`).
- 세 게이트 중 하나라도 막혀 있으면 **애초에 카드를 올리지 않는다.** 결정 블록은
  게이트를 통과한 카드의 형식 요건이지 게이트를 우회하는 통로가 아니다.

결정 블록이 없거나 선택지 행이 빠진 카드는 **유효한 카드가 아니다.** 그 상태로 사람의
응답을 받았다면 그 응답을 수락 증거로 쓰지 않고, 카드를 다시 만들어 올린다.

## Quest card and intake

카드는 `ask` 표면으로 올린다. 카드는 **기록 행 + 본문 보고 + 결정 블록** 셋으로
이뤄진다.

기록 행은 원장과 **동일 집합**이다. 이것이 나중에 원장에 들어가는 전부다.

| quest_id | source | state | pr | closure_cause |
|---|---|---|---|---|

### 본문 보고 — 열한 슬롯, 전부 필수

**사람이 제목만 보고 수락·거절·보류를 결정하게 만들지 않는다.** 다섯 필드는
원장 스키마지 보고서가 아니다. 카드마다 아래 열 슬롯을 전부 채운다. 알 수 없는 슬롯은
빼지 않고 "미확정 — 이유"를 적는다. 본문 보고는 **로컬 전용**이며 원장이나
관측 노트에 복사하지 않는다.

| # | 슬롯 | 담을 것 |
|---|---|---|
| 1 | 증상 | 한 줄로 "무엇이 어떻게 깨지는가" |
| 2 | 등급 근거 | `HIGH`/`LOW`와 그렇게 판정한 이유(크래시·데이터 손실·라이브 장애 여부) |
| 3 | 증거 | 발생 횟수·기간·마지막 발생 시각과 파일:행 지시자. 서술이 아니라 지시자다 |
| 4 | 재현 | 정확한 조건·명령과 재현률, 깨끗한 상태에서의 결과 |
| 5 | 유효성 | 현행 dev head sha와 확인한 파일:행, 이미 착륙 여부, 선점 조회 결과 |
| 6 | 영향 반경 | 누가 어떻게 다치는가. 상태 오염·수동 복구 필요 여부 |
| 7 | 원인 | **측정한 것과 추정한 것을 분리해 적는다.** 모르는 것은 모른다고 적는다 |
| 8 | 경로와 사람의 다음 동작 | `route:high-native`면 사람이 실행할 정확한 명령. `route:low-pr-proposal`면 고칠 파일 범위·필요한 테스트·전환 가드 필요 여부 |
| 9 | 업스트림 방향 정합 | 인용하는 관측 노트 추단 ID, 배제 해당 여부, 거절 위험 |
| 10 | 비용과 난이도 | 고치는 데 드는 범위와 리뷰 부담 |
| 11 | 보류 비용 | 지금 안 하면 어떻게 되는가. 기다리는 것이 안전한가 |

카드가 둘 이상이면 슬롯을 각각 다 채운 뒤, **슬롯 2·6·9·10·11을 가로로 놓고 비교하는
표**를 한 번 더 제시해 한눈에 고를 수 있게 한다.

고정 선택지는 셋이다: **수락 · 거절 · 보류.** 셋 전부에 대해 **Decision block**을
채운다 — 각각 무엇을 초래하고 뭐가 유리하고 불리한지, 그리고 권장 하나와 근거.

- 수락 → `QTR-03`. 단 현재 `accepted` 행이 있으면 `QTR-04`로 `hold:slot-occupied`.
  **두 번째 `accepted` 행은 절대 만들지 않는다.** 슬롯이 차 있으면 그 사실과 점유 중인
  quest ID를 수락 행의 "고르면 벌어지는 일"에 명시한다 — 사람이 수락을 골랐는데
  보류가 되는 것을 고르기 전에 알아야 한다.
- 거절 → `QTR-01`.
- 보류 → `QTR-02`.

원격 `open`이나 defer된 행은 **감시 의무이지 활성 WIP가 아니다.** 그래서 Tier-3 큐에서
며칠 묵는 PR이 새 로컬 작업을 막지 않는다.

## HIGH — native crash-report route

`route:high-native`.

0. **Pre-intake validity 4번(지문 존재)를 먼저 통과했는지 확인한다.** 저널에 지문이
   없으면 `hold:crash-signature-unavailable`로 보류하고 여기서 끝난다. 게이트를 보지도
   않고 리포터도 호출하지 않는다.
1. `maintainer_prohibition` → `freeze` → contract/state를 **그 순서대로** 확인. 첫
   실패에서 `QTR-13`. `reprohibited`면 **리포터를 호출하지 않는다.**
2. 세 게이트를 다 통과하면 **호출 자체를 사람에게 넘긴다.** 스킬은 `gjc crash report`를
   직접 실행하지 않는다 — 게이트가 통과됐다는 사실과 대상 지문을 제시하고, 사람이
   그 명령을 실행한다. 지문·이슈 생성을 대신 구현하지 않고, 내부 소스를 import하지 않고,
   터미널 산문을 파싱하지 않고, 종료 코드로 토큰을 추론하지 않는다.
3. **네이티브가 하는 모든 변경 확인도 사람이 한다.** 스킬은 확인을 대신 누르지 않는다.
4. 정확한 토큰이 관측되면 **`QTR-06`으로** 아래 정본 매핑을 적용한다. 관측되지 않으면
   `QTR-07`로 `hold:native-outcome-unobservable` — **추측 금지.**
5. locator가 생긴 뒤의 원격 현재 상태·종단 사유 반영은 `QTR-06`이 아니라 **`QTR-08`
   전용**이다. `QTR-06`은 네이티브 토큰만, `QTR-08`은 GH 파생만 다룬다.

| 네이티브 결과 | 논리 상태 | provenance / locator | 전이 |
|---|---|---|---|
| `no-signatures` | `closed` | `crash-report:no-signatures`, locator 없음 | `QTR-06` |
| `non-interactive` | `accepted` | `pending:crash-report:non-interactive`, 로컬 초안 | `QTR-06` |
| `cancelled` | `closed` | `crash-report:cancelled` | `QTR-06` |
| `acknowledged` | `closed` | `crash-report:acknowledged` | `QTR-06` |
| `refused` | `closed` | `crash-report:refused`; 이후 사람의 부활 허용 | `QTR-06` |
| `unmatchable` | `closed` | `crash-report:unmatchable` | `QTR-06` |
| `manual` | `accepted` | `pending:crash-report:manual`, 로컬 스냅샷 | `QTR-06` |
| `duplicate` | `open` | `crash-report:duplicate`; 알면 `issue:<ref>` | `QTR-06` |
| `commented` | `open` | `crash-report:commented`; `issue:<ref>` | `QTR-06` |
| `created` | `open` | `crash-report:created`; `issue:<ref>` | `QTR-06` |

**관측성 확인:** 현재 CLI는 내부 반환 union을 소비하며 구조화된 결과 토큰을 노출하지
않는다. 그러므로 매 호출에서 현재 문서화·구조화된 동작만 신뢰한다. 관측 불가는 추론으로
"해결"하지 않고 드러내어 보류한다.

**상태만 보면 손실되는 쌍:** `accepted`는 (`non-interactive`, `manual`).
`open`은 (`duplicate`,`commented`), (`duplicate`,`created`), (`commented`,`created`).
`closed`는 `no-signatures`·`cancelled`·`acknowledged`·`refused`·`unmatchable`의 모든 쌍.
`rejected`와 `merged`에 대응하는 네이티브 결과는 없다. 손실은 감수한다 — 로컬 구분은
네임스페이스된 `closure_cause`가, 원격 구분은 locator와 GH가 보존한다. 여섯 번째 필드는
확정된 5필드 동일성을 위반하므로 만들지 않는다.

## LOW — completed PR proposal

`route:low-pr-proposal`.

1. `maintainer_prohibition` → `freeze` → contract/state를 **그 순서로**. 첫 실패에서
   `QTR-13`이며 PR을 만들지 않는다. 그동안 로컬 완성·현재 dev 검증·배제 작업·수락·
   원장 기록·큐 대기는 계속한다.
2. **draft가 아니라 완성된 PR**을 낸다. 미완성 PR로 오너 큐를 오염하지 않는다.
2′. **결함 하나, 최소 수정, 제품 semantic 발명 금지.** 아래 Minimality gate를 통과하지
   못하면 PR을 내지 않는다.
3. **범위 밖 발견은 PR에서 판단을 구하지 않는다.** 고치다 더 근본적인 개선점을 찾았으면
   본문에는 한 줄로 "이번 scope 밖이라 별도 이슈로 분리합니다"만 적고 끝낸다.
   오너에게 결정을 요구하는 섹션을 PR에 넣지 않는다 — 리뷰어가 PR 하나에서 코드 승인과
   설계 결정을 동시에 하게 만드는 것이 큐를 막는다. 분리한 이슈는 기여물이지 문의가
   아니므로 열 수 있고, Submission ask를 거쳐 사람이 승인한 뒤에 낸다. 사전 허가를
   따로 요청하지 않는다 — 이 저장소에 그 채널은 없다.
4. 제출 직전에 **현재** upstream PR 템플릿과 기여·워크플로 지침을 다시 읽는다. 그때의
   문법과 독립 리뷰 규칙 전체를 지킨다.
5. 본문에 **verdict 블록을 현재 exact head에 바인딩**해 넣는다. 제출 시점 템플릿
   버전이 요구하는 본문을 그대로 지키는 것이 원칙이고, 이 스킬이 거는 부가 불변은
   verdict가 **지금 이 head sha에 바인드되어야 한다**는 것이다:

   ```
   gajae.pr-review-verdict.v1 <merge-approved|merge-blocked|needs-human> sha256:<exact-head> reviewer:<architect|critic|human> evidence:<...>
   ```

   독립적인 architect·critic·사람 리뷰가 없으면 `needs-human`으로 두고 멈춘다 —
   승인된 것처럼 암시하지 않는다. 자기 승인은 차단된다. head가 바뀌면 이전 sha를 핀한
   verdict는 무효다 — 재베이스했으면 블록을 새 head로 갱신한다.
6. 사용자 대면 변경은 `packages/*/CHANGELOG.md`의 `[Unreleased]`에만 적는다. 릴리스된
   섹션은 건드리지 않는다.
7. **제출은 별도의 로컬 사람 요청으로 넘긴다.** 긍정 응답은 실행을 사람에게 넘기는
   것이고 모델에게 원격 변경 권한을 주지 않는다. `QTR-05`는 사람이 만든 locator를
   독립적으로 관측해 소비한다.

## Minimality gate — 기여자는 오너의 설계 권한을 빌리지 않는다

하위 등급 PR은 제출 전에 **네 가지를 순서대로** 통과해야 한다. 첫 실패에서 단락한다.

1. **결함은 정확히 하나다.** 한 PR은 한 개의 관찰 가능한 오동작만 고친다. 같은 스캔에서
   드러난 인접 결함은 별도 후보로 분리한다 — "같은 파일을 이미 열었으니까"는 사유가 아니다.
2. **수정은 기존 계약을 복원하는 최소 변경이다.** 이미 문서화·코드화된 불변을 지키는
   방향이 유일한 정답이면 그걸 한다. 정답이 둘 이상이면 그건 결함이 아니라 **제품 결정**이다.
3. **제품 semantic을 발명하지 않는다.** 다음 중 하나라도 해당하면 그 PR은 결함 수정이
   아니라 기능 제안이다 — 코드를 내지 말고 제안만 올린다.
   - 상태 스키마에 필드·파일·테이블을 추가한다.
   - 새 정책·임계치·상한·우선순위·툴밑스톤 같은 규칙을 도입한다.
   - 데이터의 권위 관계를 바꾼다(예: 보조 기록을 새 복구원으로 승격).
   - 사용자에게 보이는 수치·문구의 의미를 재정의한다.
   - 기존 모듈 주석이 명시한 설계 의도를 부정해야 수정이 성립한다.
4. **리뷰에서 blocker가 나오면 범위를 넓히지 않는다.** blocker를 먹기 위해 새 메커니즘을
   더해야 한다면, 그건 수정이 틀렸다는 증거다. 더 작은 수정으로 돌아가거나 제안으로 내린다.

**데드라인.** 독립 리뷰를 **두 번** 돌렸는데도 blocker가 남으면 멈춘다. 세 번째 재설계는
하지 않는다 — 그 시점에 결함이 아니라 설계 문제임이 증명된 것이고, 설계는 오너 것이다.

**분리된 제안의 형태.** 제품 결정이 필요한 부분은 코드가 아니라 **별도 이슈**로 내려간다.
프레이밍은 허가 요청이 아니라 인수인계다 — *"이거 고치다 더 근본적인 개선점을 찾았는데
이번 PR scope 밖이라 별도 이슈로 뺍니다."* 이슈 본문은 세 덩어리다 — 관찰된 증거
(파일:행·재현·범위), 왜 범위 밖인지(어떤 제품 결정이 선행되어야 하는지), 다룰 수 있는
선택지와 각각의 비용. **어느 걸 고르라고 재촉하지 않고, 내가 고른 답을 기본값처럼 적지
않는다.** 연결된 PR이 있으면 서로 참조만 걸고, 이슈를 PR 머지의 조건으로 만들지 않는다.

이슈도 원격 생성이므로 **Submission ask를 그대로 거친다.** 상위 등급이 아니어도 된다 —
이전의 "상위 등급이 아니면 이슈를 열지 않는다"는 제한은 이 절차가 대체한다.

## Submission ask — 사람은 의사결정만 한다

**모든 PR은 이 경로로만 나간다.**

PR이 준비되면(세 게이트 통과 + Minimality gate 통과 + 독립 리뷰 확보 + 검증 완료
+ **선점 조회 완료**) `ask` 하나를 올리고 멈춘다. 본문은 네 슬롯이며 각 슬롯을 길게 쓰지
않는다. 그 뒤에 **Decision block을 붙인다** — 슬롯을 늘리는 것이 아니라 선택지마다
한 줄씩이다.

| 슬롯 | 담을 것 |
|---|---|
| 제목 | PR·이슈 제목 그대로 |
| 내용 | 뭐가 깨졌고 뭐를 바꿔는지 2~3줄. 전문용어 최소, 변경 규모(파일·줄) 포함 |
| 검증 | 무엇을 돌렸고 결과가 무엇인지. 독립 리뷰 verdict, 부모 커밋 red 여부, **선점 조회 결과** |
| 개선방향 | 이번에 안 담은 것·남은 위험·오너가 거절할 만한 지점 |

**선점 조회는 카드의 전제조건이다.** PR이든 이슈든, 조회 없이는 카드를 올리지 않는다 —
사람은 "중복 아니다"를 확인할 수단이 없고, 승인 즉시 원격으로 나간다. 최소 세 가지를
돌리고 쿼리와 결과를 그대로 카드에 적는다.

1. 증상 문자열·심볼로 `gh issue list --search ... --state all`.
2. 같은 것으로 `gh pr list --search ... --state all`.
3. **같은 파일을 건드리는 열린 PR** — 문자열 검색은 제목만 보므로 목록을 받아 `pulls/<n>/files`로
   경로를 대조한다. 남이 같은 파일을 수정 중이면 제목이 달라도 충돌이다.

조회를 빼먹고 제출했다면 **즉시 소급으로 돌리고 결과를 보고한다.** 깨끗하게 나왔다는 사실이
절차를 지켰다는 증거가 되지 않는다 — 운이 좋았다고 적는다.

선택지는 세 개 고정: **제출 · 보류 · 폐기.** 여러 PR이 준비되어도 카드는 하나씩만 올린다.
**셋 전부에 대해 Decision block을 채우고 권장 하나를 달아야 카드가 성립한다.**

- **제출** — 사용자 승인이므로 내가 push와 PR 생성을 실행한다. 이 지시가
  `constraint:no-auto-destructive`를 이 경로에 한해 대체한다 — **승인 없는 제출은 여전히 금지**고,
  승인은 그 카드의 그 head sha에만 유효하다. head가 바뀌면 다시 묻는다.
- **보류** — `QTR-02`, 브랜치는 유지.
- **폐기** — `QTR-01`, `exclusion:human-reject:<reason>`.

이 카드의 Decision block은 다음을 각각 구체적으로 답해야 한다 — 제출하면 오너 큐에
무엇이 쌓이고 거절 위험은 어느 정도인지, 보류하면 무엇이 상하는지(dev 이동으로 인한
재검증 비용·선점당할 위험), 폐기하면 지금까지의 검증 중 무엇이 버려지고 그 결함은
누가 계속 맞게 되는지.

원격 전달은 이미 건강하게 구성된 알림 배선이 있을 때만 그 경로를 탄다. 없거나
비정상이면 실패 증거를 남기고 로컬 카드로 끝낸다 — 배관을 새로 만들지 않는다.

## LOW-only resume and pre-submission gates

하위 등급 경로에만 적용한다. 작업을 재개하는 시점과 제출 직전에 매번 수행한다.

1. **실제 dev 확인.** 원격 실제 head는 이 명령으로 본다.

   ```sh
   git ls-remote origin refs/heads/dev
   ```

   로컬 `origin/dev`를 믿지 않는다 — refspec이 `main`만 추적하면 낡은 채 "최신"이라고
   거짓 보고한다. 갱신에는 **이 명시 refspec 명령을 쓴다.** 이것이 요구되는 형태다.

   ```sh
   git fetch origin 'refs/heads/dev:refs/remotes/origin/dev'
   ```

   전체를 새로 받아야 하는 별도 사정이 있을 때만 `git fetch origin`(인자 없는 전체
   fetch)을 대신 쓸 수 있다. 그것은 상위집합이라 `origin/dev`도 갱신되지만, 기본은
   위의 명시 refspec이며 "둘 중 아무거나"가 아니다.
   부분 fetch(`git fetch origin dev`)는 **금지다** — `FETCH_HEAD`만 갱신하고
   `origin/dev`를 그대로 두므로 거짓 최신 보고의 원인이 된다.
2. **현재 dev에서 재현·재검증.** 로트 수명 일수를 세지 않는다. 재개 시점과 제출
   직전이라는 **이벤트**가 기준이다.
3. **exact-head 증거.** upstream CI는 head가 PR 이벤트 base를 포함하지 않으면 제품
   테스트 전에 실패한다. 재검증 증거를 verdict 블록의 현재 exact head sha에 핀한다.
4. **전환 가드 동일 PR 포함.** 공유 의미론(병합 동작·CI·`.gitattributes` 등)을 바꿀
   때는 "잘못 처리되면 어떻게 되는가"를 테스트로 만들어 **같은 PR에** 넣는다.
   못 만들겠으면 아직 준비가 안 된 것이다(#3932).
5. **선점 조회 1회.** 보류 큐 항목에 대해 제출 직전에 `gh`로 중복을 한 번 확인한다.
   수동 스캔에도 이 확인을 포함한다. **상시 감시는 하지 않는다.**

## 제출 후 현황 통보 — 사람이 시각을 정한 1회 보고

기존 원격 조항(동결 감지, Submission ask)은 **사건이 일어난 순간**의 통보다. 제출한
뒤 시간이 지나 **경과 현황**을 알고 싶은 경우는 그 둘로 덮이지 않는다. 이 절이 그
빈 칸이며, 예외의 폭을 좁게 고정한다.

### 여섯 조건 — 순서대로, 첫 실패에서 예약하지 않는다

1. **사람이 시각을 지정했다.** 내가 자발적으로 만들지 않는다. "나중에 확인해서
   알려달라"는 지시가 있어야 성립한다.
2. **대상은 내 제출물 하나다.** locator(`pr:<ref>` 또는 낸 이슈 번호)가 이미 있는
   퀘스트 1건. 남의 항목이나 미할당 이슈 일반을 훑는 것은 폴링이며 계속 금지다.
3. **1회성이다.** 반복 주기를 걸지 않고 실행 후 예약이 소멸한다. 무응답을 사유로
   재예약하지 않는다 — 독촉 금지가 구조로 막혀 있어야 한다.
4. **읽기 전용이다.** 읽기 전용 `gh` 조회만. 통보 경로에서 원격 상태를 바꾸지 않는다.
5. **전송로가 이미 건강하다.** 배관을 새로 만들지 않는다(기존 조항 그대로).
6. **예약 전에 전송을 실측 검증한다.** 한 번 실제로 보내 도달을 확인한다. 검증 없이
   걸면 지정 시각에 조용히 실패해도 알 방법이 없다 — 침묵은 성공과 구별되지 않는다.

### 전송 수단 — 승인된 표면이 없다는 사실을 먼저 적는다

두 요구가 상충한다. **보장은 세션 밖에 있어야 하고, 승인된 전송 표면은 세션 안에만
있다.**

| 수단 | 세션 소멸 후 | 성격 |
|---|---|---|
| 세션 도구(파일 전송) | 사라짐 | 승인된 표면 |
| `cron` 도구 | 사라짐 | 거짓 약속 — 정기 배치 금지의 원래 근거 |
| 호스트 타이머(`at`·systemd) + `gjc notify test` | 남음 | **degradation** — `test`는 진단 액션이지 알림 API가 아니다 |

`gjc notify`에 일반 send 액션은 없다(`setup|status|health|test|recovery|bind-thread|activate-thread|daemon-internal`).
따라서 세션 밖 보장을 택하면 진단 액션을 전송로로 쓰는 것이고, 그것은 **감춰서는 안 되는
강등**이다. 규칙은 이렇다 — 세션이 살아 있는 동안의 보고는 세션 도구로 하고, 세션
소멸에도 보장이 필요하면 호스트 타이머를 쓰되 `degrade: notify_test_transport`를 남기고
그 강등 사실을 사람에게 먼저 알린다. 둘 다 불가면 예약하지 않고 로컬 보류한다.

### 원격 어휘 — 닫힌 집합을 재사용한다

이 스킬의 다섯 필드가 이미 닫힌 집합이라 그대로 원격 어휘가 된다.

**보낼 수 있다**: `quest_id` · `state`(다섯 상태) · `grade` · locator 번호와 URL ·
`closure_cause`(예약 사유) · 댓글 수 · 반응 수 · 라벨 · 마지막 갱신 시각 ·
오너/외부 댓글의 작성자와 발췌.

**로컬 전용**: Decision block과 권장 · 관측 노트 추단 · 파일 경로 · 오류 원문 · 코드와
diff 내용 · 협상 문구 · 내부 판단 근거. 원격은 *무슨 일이 있었나*만 전달하고 *무엇을
할까*는 로컬에서 정한다.

GitHub 타임스탬프는 UTC이므로 사람이 읽는 지역시간으로 변환해 보낸다.

### 통보는 상태를 만들지 않는다

통보로 알게 된 원격 변화는 그 자리에서 전이를 일으키지 않는다. 상태 갱신은 수동
스캔에서 `QTR-08`(원격 재조정)로만 한다. 여섯 번째 필드도, 새 표도, 통보 전용 상태도
만들지 않는다. 원격 응답은 수락·승인의 증거가 아니다 — Submission ask는 계속 로컬
카드다.

조회가 실패해도 실패 사유를 담아 보낸다. 조용히 넘기면 사람이 "변화 없음"으로
오독한다.

## Feedback, defer, and revival

- **거절 시퀀스:** 같은 source·같은 유형이 **3회 연속** 거절되면 `QTR-14`로 관측을
  append하고 그 유형을 **기회적 탐지에서만** 억제한다. 수동 스캔에서는 계속 조회한다.
  오탐에 수치 상한은 없다 — 거절은 원장에 `rejected`로 쌓기만 한다.
- **defer:** 읽기 전용 GH 증거가 defer로 인한 PR 폐기를 지지하면 `QTR-09`로 예약
  `tier3_defer`를 파생하고 `pending:defer-area:<ref>`를 로컬 provenance로 붙인다.
- **오너가 그 영역을 건드림:** 수동 스캔이나 허용된 퀘스트 처리 호출 중에만 `QTR-10`
  으로 `pending:revival:<evidence-ref>`를 붙인다.
  **`pending:revival:*`은 증거이고 상태도 수락도 아니다.**
- **부활:** `accepted` 행이 없고 사람이 선택할 때만 `QTR-11`로 같은 quest ID를
  `accepted`로 되돌린다. 슬롯이 차 있으면 `QTR-04`로 보류를 유지한다.
- **승계:** 오너가 내 커밋을 자기 PR로 가져간 경우 `QTR-12`로 locator를 크레딧된
  승계본으로 향하게 하고 `pending:succession:<ref>`를 붙인다. **크레딧 이의는 제기하지
  않는다** — 승계본이 `-x` 속성과 기여자를 이미 유지하면 실익이 없다.

## Ledger attachment and authority

`~/.gjc/agent/state/gjc-quest-ledger.md`에 다음 섹션을 붙인다. 그 파일에 이미 다른
기록이 있으면 훼손하지 않는다. 다른 파일도, 새 JSONL도 만들지 않는다.

```markdown
## gjc-quest 원장

| quest_id | source | state | pr | closure_cause |
|---|---|---|---|---|
```

- **열은 정확히 이 다섯 개다.** 여섯 번째 열도, 유사 상태도 없다.
- **멱등:** 같은 quest ID는 전이마다 한 행을 갱신한다. 중복 행을 쌓지 않는다.
- **로컬만 적는다:** 로컬 결정과 네임스페이스된 경로·defer·부활 provenance.
- **원격은 파생한다:** 현재 상태와 종단 사유는 읽기 전용 `gh`로 계산한다. 두 번째
  권위 상태를 마크다운에 복사하지 않는다(이중 기장 금지).
- **집계:** "내 절차 중 어떤 규칙이 샜나"는 예약된 네 맨몸 값만 세고 분모는 전체 PR
  폐기다. 콜론을 포함한 토큰은 절대 들어가지 않는다.

### 첨부 절차 — 쓰기 전에 반드시 다시 읽는다

원장 섹션과 관측 노트 섹션은 **형제 관계**이고 정본 순서는 `원장` 다음 `관측 노트`이다.
쓰기 전에 대상 파일을 다시 읽고 네 가지 형태 중 하나로 판정한다.

**네 형태를 이 순서로 평가하고 첫 일치에서 멈춘다.** 손상 판정이 항상 먼저다.

| 순서 | 관측된 형태 | 행동 |
|---|---|---|
| 1 | 같은 제목이 둘 이상 있거나, 표 머리글이 다섯 열과 다르거나, 정본 순서가 애매하거나, 파싱되지 않음 | **아무것도 쓰지 않는다.** `hold:state-malformed`로 보류하고 사람에게 보고한다 |
| 2 | 두 섹션이 정확히 하나씩 존재하고 머리글이 유효 | 해당 표에만 append·갱신한다 |
| 3 | 한쪽만 존재하고 그 한쪽이 유효 | 없는 쪽만 정본 순서 위치에 만들고, 있는 쪽은 건드리지 않는다 |
| 4 | 둘 다 없음 | 기존 내용 **끝에** 정본 순서로 새로 만든 뒤 기록한다 |

제목 판정은 **정확한 문자열 일치**로 한다. 정본 제목은 `## gjc-quest 원장`과
`## gjc-quest 업스트림 관측 노트`이며 접미어를 붙이지 않는다 — 접미어를 붙이면 다음 호출이
같은 섹션을 못 찾고 중복을 만든다.

- 어떤 경우에도 기존 섹션을 재배열·재작성·삭제하지 않는다.
- 파일을 읽지 못했으면 쓰지 않는다. 마지막으로 읽은 내용을 기억에서 재구성해 쓰지
  않는다.
- 손상·중복 상태에서 "고쳐 쓰기"를 시도하지 않는다. 이것은 셋째 게이트
  (contract/state validity) 실패이며 `QTR-13`이 적용된다.

## Upstream observation notes and persisted gates

같은 파일에 붙인다. 표 형태를 그대로 쓴다.

```markdown
## gjc-quest 업스트림 관측 노트

### 관측 — append-only
| observation_id | date | event | evidence_ref | area | observed_fact |
|---|---|---|---|---|---|

### 추단 — revisable, observation-backed
| inference_id | date | event | evidence_refs | inference | operational_effect |
|---|---|---|---|---|---|

### 파생 배제 뷰
| exclusion_class | inference_refs | current_action |
|---|---|---|

### 현재 위험 게이트 — newest qualifying evidence wins
| gate | value | evidence_refs | derived_at_event |
|---|---|---|---|
```

- **관측과 추단을 물리적으로 분리한다.** 섞인 "메모" 행을 쓰지 않는다.
- `observed_fact`에는 **직접 사건만** 적는다 — 인용, 관측된 행동, 저장소 상태.
  과부하·원인·의도·선호·일반화·예측은 전부 `inference`에만 적는다.
- `evidence_ref`는 서술이 아니라 **해석 가능한 지시자**여야 한다(PR·이슈 번호,
  파일 경로와 행, 커밋 sha). "오너 통보문" 같은 서술은 지시자가 아니다.
- 추단 행은 이미 존재하는 관측 ID·증거만 인용한다. 파생 배제나 게이트 행은 추단·증거를
  인용할 수 있지만 **사실을 새로 만들 수 없다.** 배제 행이 인용하는 추단은 그 배제의
  실제 근거를 담은 추단이어야 한다 — 주제가 다른 추단을 갖다 붙이지 않는다.
- 새 관측이 옛 사실과 모순되면 지우지 않는다. 추단 행을 교체·승계하고 파생 뷰를
  다시 만든다. **최신 자격 증거가 현재 값을 지배한다.**
- 갱신은 퀘스트 처리 시 부수적으로만 한다. 증거 없는 정기 생산은 금지.
- **매 경로 전에** 이 관측 노트를 다시 읽고, `maintainer_prohibition` 게이트 행과 그
  근거 관측을 읽고, 최신 자격 freeze·thaw 증거에서 freeze를 파생한다.
  `maintainer_prohibition`은 기본 `lifted`이고, 재금지 술어가 관측되면 `reprohibited`로
  뒤집힌 뒤 사람의 `prohibition_inquiry=done` 기록으로만 되돌아온다. 그 기록이 없거나
  깨졌거나 애매하거나 추론된 것이면 계속 `reprohibited`다. freeze는 없거나 애매하면
  `unknown`이고 계속 막는다.

## Worked decision fixtures

**설계 픽스처이자 탁상 확인이며 런타임 증명이 아니다.** 각 항목은 정본 전이를 인용하고
표현 가능한 논리 행으로 끝난다. 첫 실사용 증명은 별개다.

### A. 프로세스 시작 시 `.env` 스냅샷이 Codex 레인을 조용히 없앤다

`source:session-misbehavior`로 분류. 디스크 토큰이 +238h로 갱신됐는데 실행 중 프로세스는
1.2h 전 만료 토큰을 들고 있어 모든 `codex-*` 레인이 조용히 사라졌다.
`grade:LOW` — 크래시·데이터 손실·라이브 장애가 아닌 조용한 능력 상실.
배제 해당 없음(실제 프로바이더 레인 능력을 바꾸며 피해자 없는 진단이 아니다).
유효성·세 게이트를 모두 통과하고 `accepted` 행이 없으므로 `QTR-03`으로 수락된다.
이미 다른 `accepted` 행이 있었다면 `QTR-04`로 `hold:slot-occupied`였을 것이다.

| quest_id | source | state | pr | closure_cause |
|---|---|---|---|---|
| `GQ-20260812-env-snapshot` | `session-misbehavior` | `accepted` | `—` | `pending:new` |

이 카드의 Decision block은 이런 모양이다. 세 행이 다 차 있고, 권장은 표에 이미 적은
사실만 인용하며, 갈리는 조건을 숨기지 않는다.

| 선택지 | 고르면 벌어지는 일 | 유리한 점 | 불리한 점 |
|---|---|---|---|
| 수락 | `QTR-03`으로 `accepted`. 슬롯을 점유하고 내가 수정을 시작한다 | 재현 절차가 이미 서있고 수정 범위가 파일 하나로 좁다 | 슬롯이 하나뿐이라 더 급한 것이 나와도 `QTR-04`로 밀린다 |
| 거절 | `QTR-01`로 종단. 같은 quest ID를 사람이 되살리기 전까지 끝 | 오너 방향과 어김지는 후보를 일찍 정리한다 | 조용한 능력 상실은 사용자가 알아채지 못해 재발을 관측할 수도 없다 |
| 보류 | `QTR-02`로 `open` + `hold:<reason>`. 슬롯을 비워 둔다 | 더 높은 등급 후보가 먼저 들어올 수 있다 | dev가 움직이면 재개 시점에 유효성 확인을 처음부터 다시 해야 한다 |

권장: **수락.** 수정 범위가 좁고 재현이 서 있어 슬롯 점유 기간이 짧을 것으로 보인다.
다만 더 높은 등급 후보가 이미 대기 중이라면 보류가 맞다 — 이 카드만 보고는 그걸 알 수 없다.

### B. 실패한 서브에이전트가 auth 오류를 숨긴다 — 의도적으로 EXCLUDED

`Task failed; error recorded`가 전부였고 전체 조회에도 오류 본문이 없어 JWT `exp`를
손으로 디코딩해야 원인이 나왔다. 후보는 "내부 실패·auth 가시성 개선"이다.
`grade:LOW`. **배제 2번(피해자 없는 내부 진단·가시성)에 해당한다.**
작성자가 내고 싶어도 배제된다 — 열의가 퀘스트가 되기 전에 필터가 물어야 한다.
수동 스캔에서 명시적으로 평가했으므로 학습용으로 거절을 기록한다(`QTR-01`).

| quest_id | source | state | pr | closure_cause |
|---|---|---|---|---|
| `GQ-20260812-auth-visibility` | `session-misbehavior` | `rejected` | `—` | `exclusion:victimless-internal-diagnostics-visibility` |

### C. `gjc -p` retained-authority 크래시

`source:crash`. 설치본 0.13.1에서 `binding_invalid: prepare:retained_authority`가
`session-manager.ts:3234` → `managed-session-scope.ts:1075` 경로로 재현된다.
`grade:HIGH` — 결정론적 프로세스 크래시. 배제 해당 없음. 세 게이트도 모두 통과.
**그러나 Pre-intake validity 3번에서 걸린다** — 기존 agent 디렉터리에서만 재현되고
새 agent 디렉터리에서는 재현되지 않아 트리거 상태가 특정되지 않았다. 카드로 올리지
않고 보류한다 — 동작하는 지문을 모르는 채 리포터를 돌리면 오너 큐를 오염한다.

| quest_id | source | state | pr | closure_cause |
|---|---|---|---|---|
| `GQ-20260812-binding-invalid` | `crash` | `open` | `—` | `hold:dev-reproduction-unverified` |

트리거 상태가 특정돼 카드로 올라가고 사람이 수락·실행한 뒤
`created`가 관측되면 `QTR-06`으로 논리 상태 `open`, `closure_cause`에
`crash-report:created`, `pr`에 알려진 `issue:<ref>`를 기록한다. 그 이후의 원격 현재
상태와 종단 사유는 `QTR-08`이 읽기 전용 `gh`로만 파생하며 마크다운에 두 번째 권위
상태로 복사하지 않는다.

## Prohibitions and boundaries

전부 금지다. 예외 경로를 두지 않는다.

- **오너에게 허가를 묻지 않는다** — "내도 되나요", "어떤 걸 원하세요", 금지 해제 문의,
  자동 발송 초안 전부 금지. **예외는 기여물뿐이다** — 증거를 담은 범위 밖 결함·개선
  이슈는 문의가 아니라 인수인계이며, Submission ask 승인 후에 낼 수 있다.
  그 이슈도 답변을 독촉하거나 PR 머지의 조건으로 삼지 않는다.
- **정기 배치를 만들지 않는다** (`cron`은 세션과 함께 소멸한다). 반복 주기는 계속
  금지다. 사람이 시각을 지정한 **1회** 현황 보고만 예외이며 조건은 제출 후 현황 통보
  절에 있다.
- **upstream 미할당 이슈를 자동 폴링하지 않는다.** locator가 있는 내 제출물 1건을
  사람이 지정한 시각에 한 번 읽는 것은 폴링이 아니다. 대상을 넓히거나 주기를 걸면
  폴링이 된다.
- **수치 자원 상한을 쓰지 않는다** — WIP 개수, 로트 수명 일수, 토큰 예산 전부 없다.
  **단 두 개의 탐지 술어(3회 연속 거절, 24시간 내 다수 close)는 상한이 아니라 탐지
  조건이며 숫자 그대로 유지한다.**
- **크래시 지문·이슈 보고를 재구현하지 않는다** — `gjc crash report`를 감싼다.
- **새 JSONL 파일을 만들지 않는다.** 이 절차를 검사하는 스크립트도 만들지 않는다.
  사람이 지정한 1회 현황 보고를 실행하는 스크립트는 절차 검사기가 아니라 원격 상태
  보고자이므로 예외이며, 그 수명은 예약 1회로 끝난다.
- **draft PR로 선점하지 않는다** — 미완성 제출물이 쌓이면 리뷰 큐가 통째로 정리된다.
- **자동 제출하지 않는다.** 원격 상태를 바꾸는 조작은 Submission ask 승인 없이
  실행하지 않는다. 승인은 그 카드의 그 head sha에만 유효하며, 범위가 달라지거나
  head가 움직이면 다시 묻는다 (`constraint:no-auto-destructive`).
- **선점을 상시 감시하지 않는다** — 제출 직전 1회와 수동 스캔뿐이다.
- **선택지만 나열한 카드를 올리지 않는다.** Decision block 없이, 또는 고정 선택지 중
  일부만 분석해 올리지 않는다. 권장을 생략하거나 "알아서 고르세요"로 대체하지
  않는다 — 근거가 부족하면 그 사실과 무엇이 부족한지를 적는다.
- **권장을 승인으로 간주하지 않는다.** 내가 권한 선택지를 사람이 고르기 전에 실행하지
  않고, 무응답을 동의로 해석하지 않는다.
- **오너에게 나가는 이슈·PR 본문에는 권장을 적지 않는다.** Decision block은 로컬
  사람 전용이며, 원장·관측 노트에도 복사하지 않는다.
- **크레딧 이의를 제기하지 않는다.**
- 훅을 설치하거나 자동 콜백을 약속하지 않는다.
- 설정 파일을 바꾸지 않는다.
- 손상·중복 상태를 고쳐 쓰지 않는다 — 보류하고 사람에게 보고한다.

### Escalation — 고쳐 쓰지 말고 멈출 조건

다음이면 로컬 보류로 fail closed하고 architect·critic 리뷰를 요청한다.
여섯 번째 필드나 상태가 필요해 보인다 · 네이티브 결과가 관측 불가인데 산문·종료 코드를
파싱하자는 제안이 나온다 · 현재 PR·워크플로 지침이 exact-head·사람 리뷰 불변과 충돌한다 ·
상태 섹션이 중복·손상됐다 · 문의 증거가 없거나 애매한데 추론하자는 제안이 나온다 ·
스킬이 오너에게 연락하거나 자동 발송 문의를 작성하라고 요구받는다 · 1회 현황 보고가
반복 주기·다중 대상·쓰기 조작으로 번지려 한다 · 다른 게이트 증거가
없거나 애매하다 · 고정 선택지 중 어느 하나의 결과를 진지하게 서술할 수 없어 Decision
block을 채울 수 없다 · 활성화를 위해 스킬이 설정을 바꿔야 할 것처럼 보인다 · 마크다운에
자동 콜백을 요구받는다 · 상시 백그라운드 서비스나 절차 체커나 반복 타이머가 제안된다
(사람이 지정한 1회 예약은 해당하지 않는다).
